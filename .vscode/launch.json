{
    "version": "0.2.0",
    "configurations": [
        



        {
            "autoAttachChildProcesses": "node-terminal",
            "name": "Run Script: start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}